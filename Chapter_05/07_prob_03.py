# Yes we can have a set with 18(int) and "18"(str) as a values in it
# lets see with an example
 
s = (18,"18")
print(s)  

# the reason behind it is python sees an element with diffrent datatype as a different element
 